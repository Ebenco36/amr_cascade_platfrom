Legacy scripts in this directory are retained for audit only.

They are not part of the active production pipeline. In particular, the current
prevalence workflow uses model-free eligible-denominator bounds plus
cascade-aware MNAR sensitivity curves. Earlier empirical-delta projection
diagnostics are kept here only so old exploratory figures remain reproducible.
